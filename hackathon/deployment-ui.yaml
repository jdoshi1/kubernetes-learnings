kind: Deployment
apiVersion: apps/v1
metadata:
  name: rvstore-ui-deployment
  labels:
    app: rvstore-ui
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rvstore-ui
  template:
    metadata:
      labels:
        app: rvstore-ui
    spec:
      containers:
      - name: rvstore-ui
        image: vergeops/k8s-rvstore-ui:1.0
---
kind: Service
apiVersion: v1
metadata:
  name: rvstore-ui
spec:
  selector:  
    app: rvstore-ui
  ports:
  - protocol: TCP
    port: 80
    nodePort: 30080
  type: NodePort  
